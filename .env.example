# SQLite database path
# For local development, this defaults to ./data/listings.db
# For Render, set this to /data/listings.db and configure a persistent disk mounted to /data
DATABASE_PATH=./data/listings.db

# Server configuration (Render injects PORT automatically)
PORT=8000
